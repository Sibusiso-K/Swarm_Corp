# Copy this file to .env and paste your key in — never commit .env itself.
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=

# Leave as "free" — see CLAUDE.md. The paid/anthropic tier is dormant code,
# not a real feature. Do not set this to anything else without re-reading
# CLAUDE.md's cost constraints first.
SWARM_TIER=free
