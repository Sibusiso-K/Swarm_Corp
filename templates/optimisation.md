Domain: Operations-research / optimisation (Google OR-Tools or equivalent).
Criteria hints: a clearly stated objective function and constraints, a
feasibility check (what happens when no solution exists — fail loudly, not
silently return garbage), and a test with a known-optimal small case to
verify the solver is actually solving the right problem.
