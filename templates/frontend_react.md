Domain: React/TypeScript frontend.
Criteria hints: typed props/state (no implicit `any`), a loading and an
error state for anything async — not just the happy path, and accessible
markup (semantic elements, not div-soup). Component-level test for the
core interaction, not a full E2E harness.
